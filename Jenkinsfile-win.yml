#Reference: https://plugins.jenkins.io/pipeline-as-yaml/
pipeline:
    agent:
        jenkinsagent:
            stages:
                - stage: "Checkout"
                  steps:
                    - echo We are In
             