pipeline:
    agent: 
        node:
            label: 'jenkinsagent'
    stages:
        - stage: "Checkout"
          steps:
            script: |
                git sparse-checkout init --cone
                git sparse-checkout add is/configurations
                mkdir is\tests